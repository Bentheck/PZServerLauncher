<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:PZServerLauncher.App.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="1320"
             d:DesignHeight="920"
             x:Class="PZServerLauncher.App.Views.LogsWorkspaceView"
             x:Name="LogsRoot"
             x:DataType="vm:LogsWorkspaceViewModel">
    <Grid RowDefinitions="Auto,*" RowSpacing="8">
        <Border Classes="workspace-band" Padding="14,12">
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="18">
                <StackPanel Spacing="8">
                    <TextBlock Classes="kicker" Text="Live Operations Console" />
                    <TextBlock Classes="panel-title" FontSize="28" Text="{Binding ConsoleHeroTitle}" />
                    <TextBlock Classes="body" Text="{Binding ConsoleHeroCopy}" TextWrapping="Wrap" />
                    <WrapPanel Orientation="Horizontal">
                        <Border Background="{DynamicResource PanelSubtleBrush}" CornerRadius="999" Padding="10,5" Margin="0,0,8,8">
                            <TextBlock Foreground="{DynamicResource TextBrush}" FontSize="11" Text="{Binding Branch}" />
                        </Border>
                        <Border Background="{DynamicResource PanelSubtleBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="999" Padding="10,5" Margin="0,0,8,8">
                            <TextBlock Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Text="{Binding ConsoleStatusSummary}" />
                        </Border>
                        <Border Background="{DynamicResource PanelSubtleBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="999" Padding="10,5" Margin="0,0,8,8">
                            <TextBlock Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Text="{Binding ActivePlayerCountSummary}" />
                        </Border>
                    </WrapPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="8" HorizontalAlignment="Right">
                    <Button Classes="action-secondary"
                            Command="{Binding ReloadCommand}"
                            Content="Reload" />
                    <TextBlock Classes="caption"
                               HorizontalAlignment="Right"
                               Text="{Binding LoadStatus}"
                               TextAlignment="Right"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" ColumnDefinitions="310,*" ColumnSpacing="12">
            <StackPanel Spacing="8">
                <Border Classes="workspace-band" Padding="12,10">
                    <StackPanel Spacing="10">
                        <TextBlock Classes="kicker" Text="Live Controls" />
                        <TextBlock Classes="body" Text="{Binding BroadcastGuidance}" TextWrapping="Wrap" />
                        <TextBlock Classes="caption" Text="{Binding PlayerModerationSummary}" TextWrapping="Wrap" />

                        <StackPanel Spacing="6">
                            <TextBlock FontWeight="SemiBold" Text="Broadcast Message" />
                            <TextBox Text="{Binding BroadcastMessage}" PlaceholderText="servermsg text" />
                            <Button Classes="action-primary"
                                    Command="{Binding SendBroadcastCommand}"
                                    Content="Send Broadcast"
                                    IsEnabled="{Binding CanSendCommands}" />
                        </StackPanel>

                        <StackPanel Spacing="6">
                            <TextBlock FontWeight="SemiBold" Text="Raw Console Command" />
                            <TextBox Text="{Binding RawConsoleCommand}" PlaceholderText="adduser / kickuser / save" />
                            <Button Classes="action-secondary"
                                    Command="{Binding SendConsoleCommand}"
                                    Content="Send Console Command"
                                    IsEnabled="{Binding CanSendCommands}" />
                        </StackPanel>

                        <WrapPanel Orientation="Horizontal" ItemHeight="34">
                            <Button Classes="action-secondary"
                                    Command="{Binding ListPlayersCommand}"
                                    Content="Request Player List"
                                    IsEnabled="{Binding CanSendCommands}"
                                    Margin="0,0,8,8" />
                            <Button Classes="action-secondary"
                                    Command="{Binding SaveWorldCommand}"
                                    Content="Save World"
                                    IsEnabled="{Binding CanSendCommands}"
                                    Margin="0,0,8,8" />
                            <Button Classes="action-secondary"
                                    Command="{Binding ReloadOptionsCommand}"
                                    Content="Reload Options"
                                    IsEnabled="{Binding CanSendCommands}"
                                    Margin="0,0,8,8" />
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <Border Classes="workspace-band" Padding="12,10">
                    <StackPanel Spacing="8">
                        <TextBlock Classes="kicker" Text="Connected Players" />
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                      VerticalScrollBarVisibility="Auto"
                                      MaxHeight="300">
                            <ItemsControl ItemsSource="{Binding ConnectedPlayers}" IsVisible="{Binding HasConnectedPlayers}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="vm:LogsWorkspaceViewModel+ConnectedPlayerRowViewModel">
                                        <Border Classes="row-strip">
                                            <StackPanel Spacing="8">
                                                <StackPanel Spacing="4">
                                                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="{Binding UserName}" />
                                                    <TextBlock Classes="caption" Text="{Binding JoinedSummary}" TextWrapping="Wrap" />
                                                    <TextBlock Classes="caption" Text="{Binding LastSeenSummary}" TextWrapping="Wrap" />
                                                </StackPanel>
                                                <WrapPanel Orientation="Horizontal" ItemHeight="34">
                                                    <Button Classes="action-danger" Command="{Binding #LogsRoot.DataContext.KickPlayerCommand}" CommandParameter="{Binding .}" Content="Kick" AutomationProperties.Name="Kick selected player" IsEnabled="{Binding #LogsRoot.DataContext.CanSendCommands}" Margin="0,0,8,0" />
                                                    <Button Classes="action-danger" Command="{Binding #LogsRoot.DataContext.BanPlayerCommand}" CommandParameter="{Binding .}" Content="Ban" AutomationProperties.Name="Ban selected player" IsEnabled="{Binding #LogsRoot.DataContext.CanSendCommands}" Margin="0,0,8,0" />
                                                    <Button Classes="action-secondary" Command="{Binding #LogsRoot.DataContext.WhitelistPlayerCommand}" CommandParameter="{Binding .}" Content="Whitelist" IsEnabled="{Binding #LogsRoot.DataContext.CanSendCommands}" />
                                                </WrapPanel>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                        <TextBlock Classes="caption"
                                   IsVisible="{Binding HasNoConnectedPlayers}"
                                   Text="No players inferred yet. Keep the console open or request the live list."
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border Classes="workspace-band" Padding="12,10">
                    <StackPanel Spacing="8">
                        <TextBlock Classes="kicker" Foreground="{DynamicResource AccentBrush}" Text="Live Ops Briefing" />
                        <TextBlock FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Text="{Binding FeedHealthHeadline}" TextWrapping="Wrap" />
                        <TextBlock Classes="body" Foreground="{DynamicResource TextMutedBrush}" Text="{Binding LiveOpsTriageSummary}" TextWrapping="Wrap" />
                        <TextBlock Classes="caption" Foreground="{DynamicResource TextSubtleBrush}" Text="{Binding RosterPostureSummary}" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <Grid Grid.Column="1" RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="8">
                <Border Classes="workspace-band" Padding="12,10">
                    <Grid RowDefinitions="Auto,*" RowSpacing="12">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <StackPanel Spacing="4">
                                <TextBlock Classes="kicker" Foreground="{DynamicResource AccentBrush}" Text="Live Stream" />
                                <TextBlock FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Text="{Binding LogStreamSummary}" TextWrapping="Wrap" />
                                <TextBlock Classes="caption" Foreground="{DynamicResource TextSubtleBrush}" Text="{Binding FriendlyFeedSummary}" TextWrapping="Wrap" />
                                <TextBlock Classes="caption" Foreground="{DynamicResource TextSubtleBrush}" Text="{Binding SignalCountSummary}" TextWrapping="Wrap" />
                            </StackPanel>
                            <Border Grid.Column="1" Background="{DynamicResource PanelSubtleBrush}" CornerRadius="999" Padding="10,5" VerticalAlignment="Top">
                                <TextBlock Foreground="{DynamicResource TextBrush}" FontSize="11" Text="{Binding ConsoleModeSummary}" />
                            </Border>
                        </Grid>

                        <Border Grid.Row="1" Classes="terminal-panel" Padding="12">
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto"
                                          MaxHeight="460">
                                <ItemsControl ItemsSource="{Binding FriendlyLogLines}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Classes="body"
                                                       FontFamily="Cascadia Code,Consolas,monospace"
                                                       Foreground="#E9DED2"
                                                       TextAlignment="Left"
                                                       Text="{Binding .}"
                                                       TextWrapping="Wrap"
                                                       Margin="0,0,0,6" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Border>

                <Grid Grid.Row="1" ColumnDefinitions="*,*,*" ColumnSpacing="14" IsVisible="{Binding HasLogs}">
                    <Border Classes="workspace-band" Padding="12,10">
                        <StackPanel Spacing="8">
                            <TextBlock Classes="kicker" Text="Recent Player Signals" />
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto"
                                          MaxHeight="220">
                                <ItemsControl ItemsSource="{Binding RecentPlayerSignals}" IsVisible="{Binding HasPlayerSignals}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Classes="caption" Text="{Binding .}" TextWrapping="Wrap" Margin="0,0,0,6" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                            <TextBlock Classes="caption" IsVisible="{Binding HasNoPlayerSignals}" Text="Join and leave signals appear here when the runtime can infer them." TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Classes="workspace-band" Padding="12,10">
                        <StackPanel Spacing="8">
                            <TextBlock Classes="kicker" Text="Recent Operator Actions" />
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto"
                                          MaxHeight="220">
                                <ItemsControl ItemsSource="{Binding RecentOperatorActions}" IsVisible="{Binding HasOperatorActions}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Classes="caption" Text="{Binding .}" TextWrapping="Wrap" Margin="0,0,0,6" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                            <TextBlock Classes="caption" IsVisible="{Binding HasNoOperatorActions}" Text="Broadcasts and raw console commands sent from the launcher will appear here as soon as the runtime is live." TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Classes="workspace-band" Padding="12,10">
                        <StackPanel Spacing="8">
                            <TextBlock Classes="kicker" Text="Live Ops Checklist" />
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto"
                                          MaxHeight="220">
                                <ItemsControl ItemsSource="{Binding LiveOpsChecklist}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Classes="row-strip">
                                                <TextBlock Classes="caption" Text="{Binding .}" TextWrapping="Wrap" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Grid.Row="2"
                        Classes="workspace-band"
                        Padding="16"
                        IsVisible="{Binding HasLogs}">
                    <StackPanel Spacing="10">
                        <TextBlock Classes="kicker" Text="Raw Buffer" />
                        <TextBlock Classes="caption" Text="{Binding RawBufferSummary}" TextWrapping="Wrap" />
                        <TextBlock Classes="caption"
                                   Text="Raw runtime output behind the friendly feed."
                                   TextWrapping="Wrap" />

                        <Border Classes="terminal-panel" Padding="12">
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto"
                                          MaxHeight="280">
                                <ItemsControl ItemsSource="{Binding LogLines}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Classes="caption"
                                                       FontFamily="Cascadia Code,Consolas,monospace"
                                                       Foreground="#E9DED2"
                                                       Text="{Binding .}"
                                                       TextWrapping="Wrap"
                                                       Margin="0,0,0,6" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </Border>

                <Border Grid.Row="3"
                        Background="{DynamicResource PanelSubtleBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,1,0,1"
                        CornerRadius="0"
                        Padding="16"
                        IsVisible="{Binding HasNoLogs}">
                    <TextBlock Classes="body" Text="No buffered logs yet. Start or reload the profile, then watch live output here." TextWrapping="Wrap" />
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
